package com.unicam.risorseLocali.Util.Enum;

/**
 * Questa enumerazione rappresenta i vari stati
 * che può avere un elemento della mappa
 */
public enum StatusElement {
    VISIBLE,
    PENDING,
    INVISIBLE;
}
